import pytest

def minimum_window_subsequence():
    """
    Given two strings s1 and s2, find the minimum window in s1 which will contain s2 in order as a subsequence.
    Return the smallest such window. If no such window exists, return an empty string.
    """
    pass

def test_minimum_window_subsequence():
    result = minimum_window_subsequence()
    assert result is None  # TODO: update assertion

if __name__ == "__main__":
    import sys
    sys.exit(pytest.main([__file__]))
