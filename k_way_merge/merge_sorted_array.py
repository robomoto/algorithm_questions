import pytest

def merge_sorted_array():
    """
    Merges two sorted arrays into one sorted array.

    Given two sorted arrays, the task is to merge them into a single sorted array while maintaining the order.

    Args:
        nums1 (List[int]): The first sorted array.
        nums2 (List[int]): The second sorted array.

    Returns:
        List[int]: The merged sorted array.
    """

    pass

def test_merge_sorted_array():
    result = merge_sorted_array()
    assert result is None  # TODO: update assertion

if __name__ == "__main__":
    import sys
    sys.exit(pytest.main([__file__]))
